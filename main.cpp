#include "JunctionLight.h"
#include "TrafficLight.h"
#include "Windows.h"
#include <iostream>

using namespace std;

int main(){
    int delay[4] = { 1,1,5,1 };

//    TrafficLight* tra = new TrafficLight(delay);
    while (true) {
//        junction->goNext();
        Sleep(200);
    }
}